#include "main.h"

/**
 * main - entry point
 * @argc: number of arguments
 * @argv: array of given arguments, represented by strings
 *
 * Return: 0, always success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
